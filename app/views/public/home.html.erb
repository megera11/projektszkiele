<!DOCTYPE html>
<html>
  <head>
    <title>Test1</title>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>
  <body>
    <div id="bigdiv">
    <div id="logo" >
    <h1>  Pokarmies</h1>
    </div>
    <div id="columns" >
      <div id="menu" >
        <h3> Logowanie </h3>
        <%= form_tag sessions_path, class: '' do %>
        <%= text_field_tag(:email, nil, placeholder: 'type your login here', class: '') %>
        <%= password_field_tag(:password, nil, placeholder: 'type your password here', class: '') %>
        <%= submit_tag("Login", class: '') %>
        <% end %>

      </div>
      <div id="content" >
        <h3> Rejestracja </h3>
        <%= form_for(:user, url: users_path) do |f| %>
          <table>
            <tr>
                <td><%= f.label :email %></td>
                <td><%= f.email_field :email %></td>
            </tr>
            <tr>
              <td><%= f.label :password %></td>
              <td><%= f.password_field :password %></td>
            </tr>
            <tr>
              <td><%= f.label :password_confirmation, "Repeat password" %></td>
              <td><%= f.password_field :password_confirmation %></td>
            </tr>
            <tr>
              <td><%= f.label :name %></td>
              <td><%= f.text_field :name %></td>
            </tr>
            <tr>
              <td></td>
              <td><%= f.submit %></td>
            </tr>
          </table>
        <% end %>

      </div>
    </div>
    <div id="stopka">
   to stopka
    </div>
    </div>
  </body>
</html>
